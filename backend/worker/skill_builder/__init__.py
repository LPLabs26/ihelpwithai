"""Skill Builder — convert how-to content into executable agent skills.

v1 scope: YouTube tutorial -> verified .skill file.
The engine is a linear pipeline (see pipeline.py). AI stages require a
developer API key; non-AI stages run without one.
"""

__version__ = "0.1.0"
