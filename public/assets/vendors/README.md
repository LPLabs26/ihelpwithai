Official vendor logo assets live here.

The site renders a clean initials fallback for any vendor registry entry whose official local logo file is not present yet. Add only vendor-provided brand/media-kit SVG or PNG files here, using the filenames listed in `src/data/site-content.mjs`.
